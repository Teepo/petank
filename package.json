{
  "name": "petank",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "github-deploy": "yarn run build --env=gh:prod && gh-pages -d dist",
    "firebase-deploy": "yarn run build && firebase deploy"
  },
  "devDependencies": {
    "firebase": "^10.0.0",
    "firebase-tools": "^12.4.3",
    "vite": "^4.4.2"
  },
  "dependencies": {
    "delegate": "^3.2.0",
    "phaser": "^3.60.0",
    "phaser3-rex-plugins": "^1.60.3",
    "sass": "^1.63.6"
  }
}
